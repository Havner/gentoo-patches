diff --git a/cmatrix.c b/cmatrix.c
index 90284cb..204b9a7 100644
--- a/cmatrix.c
+++ b/cmatrix.c
@@ -132,6 +132,7 @@ void usage(void) {
     printf(" -b: Bold characters on\n");
     printf(" -B: All bold characters (overrides -b)\n");
     printf(" -c: Use Japanese characters as seen in the original matrix. Requires appropriate fonts\n");
+    printf(" -d: Use default colors mode, e.g. don't set background making is possibly transparent\n");
     printf(" -f: Force the linux $TERM type to be on\n");
     printf(" -l: Linux mode (uses matrix console font)\n");
     printf(" -L: Lock mode (can be closed from another terminal)\n");
@@ -287,13 +288,14 @@ int main(int argc, char *argv[]) {
     int randmin = 0;
     int pause = 0;
     int classic = 0;
+    int defcolors = 0;
 
     srand((unsigned) time(NULL));
     setlocale(LC_ALL, "");
 
     /* Many thanks to morph- (morph@jmss.com) for this getopt patch */
     opterr = 0;
-    while ((optchr = getopt(argc, argv, "abBcfhlLnrosmxVu:C:")) != EOF) {
+    while ((optchr = getopt(argc, argv, "abBcdfhlLnrosmxVu:C:")) != EOF) {
         switch (optchr) {
         case 's':
             screensaver = 1;
@@ -335,6 +337,9 @@ int main(int argc, char *argv[]) {
         case 'c':
             classic = 1;
             break;
+        case 'd':
+            defcolors = 1;
+            break;
         case 'f':
             force = 1;
             break;
@@ -410,7 +415,7 @@ if (console) {
         start_color();
         /* Add in colors, if available */
 #ifdef HAVE_USE_DEFAULT_COLORS
-        if (use_default_colors() != ERR) {
+        if (defcolors && use_default_colors() != ERR) {
             init_pair(COLOR_BLACK, -1, -1);
             init_pair(COLOR_GREEN, COLOR_GREEN, -1);
             init_pair(COLOR_WHITE, COLOR_WHITE, -1);
